__pycache__/
*.pyc
.env
*.egg-info/
venv/
.venv/
node_modules/
dist/
*.db

# agent/tool temp artifacts (session-scoped, never part of the project)
.hermes-tmp*/
.claude/

# runtime artifacts (regenerated; not source)
celerybeat-schedule*
backend/celerybeat-schedule*
# local-filesystem encrypted document storage (US04 fallback; R2 is the real backend)
backend/uploads/
